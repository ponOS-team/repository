name: alpine-bootstrap-repo

on:
  push:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: alpine:3.23

    steps:
      # 1. Подготовка репозиториев Alpine
      - name: set repositories + update
        run: |
          echo "https://dl-cdn.alpinelinux.org/alpine/v3.23/main" > /etc/apk/repositories
          echo "https://dl-cdn.alpinelinux.org/alpine/v3.23/community" >> /etc/apk/repositories
          apk update

      # 2. Установка инструментов
      - name: install tools
        run: |
          apk add --no-cache git apk-tools ca-certificates

      # 3. Клонирование самого репо (локально)
      - name: clone repo
        run: |
          git clone https://x-access-token:${REPO_TOKEN}@github.com/${GITHUB_REPOSITORY}.git repo
        env:
          REPO_TOKEN: ${{ secrets.REPO_TOKEN }}

      # 4. Fetch пакетов Alpine рекурсивно
      - name: fetch base packages
        run: |
          mkdir -p repo/x86_64
          apk fetch --recursive \
            --repository https://dl-cdn.alpinelinux.org/alpine/v3.23/main \
            --repository https://dl-cdn.alpinelinux.org/alpine/v3.23/community \
            --arch x86_64 \
            --output repo/x86_64 \
            musl busybox nano
          apk index -o repo/x86_64/APKINDEX.tar.gz repo/x86_64/*.apk

      # 5. Commit & push в main
      - name: commit & push to main
        run: |
          cd repo
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add .
          git commit -m "update alpine bootstrap repo" || echo "No changes to commit"
          git push https://x-access-token:${REPO_TOKEN}@github.com/${GITHUB_REPOSITORY}.git main
        env:
          REPO_TOKEN: ${{ secrets.REPO_TOKEN }}
